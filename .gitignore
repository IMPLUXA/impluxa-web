# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase CLI temp
supabase/.temp/

# lighthouse reports
coverage/lighthouse/

# local tooling (MCP Sentinel project-level allowlist created by agents — global lives at ~/.claude/)
.security/

# design handoffs (read-only reference bundles; assets go to storage bucket, NOT committed)
.design/

# ============================================================
# Scratch operativo s33-s43 (Squad s43: BA a811a552 + SE a14d1405).
# Scoped/anclado, collision-free verificado vs `git ls-files`.
# Cubre los 49 ?? sin shadowear los 29 .sql tracked (migrations + rls).
# NO usar blanket *.sql / *.bak / *.json (rompe versionado migrations).
# ============================================================

# scratch raiz prefijo _ (_apply_/_assemble_/_gate_/_recapture_/_upload_ .mjs + _turismo_*.json)
/_*.mjs
/_*.json

# backup de refs one-off s43
/branch-refs-backup-s43.txt

# patches PowerShell operativos (scripts/ solo tiene .ts + README tracked)
scripts/*.ps1

# .planning/: NO entero (57 .md tracked dentro). Solo scratch prefijo _ + subdirs de assets.
.planning/_*
.planning/hero-4k-webp/
.planning/hero-slideshow-src/
.planning/video/
