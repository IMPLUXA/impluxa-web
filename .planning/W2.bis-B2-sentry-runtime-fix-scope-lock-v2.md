# W2.bis-B2-SENTRY-RUNTIME-CAPTURE-FIX — Scope Lock v2 POST-BASELINE

**Owner**: Senior PM FRESH (Squad T2 W2.bis-B2 reconvene)
**Date**: 2026-05-24
**Branch**: feature/sentry-verify-b2 (9 commits preservados)
**Flag**: hakuna_live=false
**Supersedes**: scope-lock v1 (B2.S1 SHIPPED + B2.S2 invalidated empirico)
**Veredicto**: recomendamos proceder

---

## 0. Empirical findings B2.S1 que invalidan plan v1

- HTTP 500 endpoint /api/sentry-verify-b2 (handler ejecuta ✓)
- SENTRY_EDGE_CONFIG_LOADED probe disparo (edge config carga ✓)
- SENTRY_SERVER_CONFIG_LOADED probe NO disparo (server config NUNCA carga ✗)
- Sentry API events last 1h: 0 events (transport end-to-end FAIL)
- `export const runtime = "nodejs"` IGNORADO por Next.js 16 + Turbopack (D5 INEFFECTIVE empirico)

**Implicacion**: hipotesis H5 v1 (diff vs canonical docs) downgraded. Root cause real = sentry.server.config.ts no se carga porque instrumentation register() no se invoca o runtime routing roto Turbopack. B2.S2 v1 obsoleta.

---

## 1. Sub-pasos B2.bis re-decomposition

| ID        | Objetivo                                                                                                                                                                                                                                | Entregable observable                                                                   | Tiempo realistico (5x) | Deps           | Abort criteria                                                                 |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ---------------------- | -------------- | ------------------------------------------------------------------------------ |
| B2.S2.bis | Probe instrumentation.ts register() invocation: agregar probe SENTRY_INSTRUMENTATION_REGISTER_INVOKED al inicio register() + redeploy + curl + grep Vercel logs                                                                         | Vercel logs row con probe string O ausencia (confirma register() never-called)          | 20 min (nominal 4)     | B2.S1 done     | Vercel deploy FAIL 2x build error → revert probe → ASK CEO bifurcacion         |
| B2.S3.bis | F2 LEAN: si register() NOT invoked → fix instrumentation.ts exports/signature per Next.js 16 instrumentation hook spec. Si register() SI invoked pero server config no carga → fix dynamic import path/runtime guard interno register() | Vercel deploy SHIPPED + curl verify-b2 + Sentry API events query event_id != null <2min | 60 min (nominal 12)    | B2.S2.bis      | Deploy FAIL 2x consec O fix aplicado + 0 events Sentry post-verify → B2.S4.bis |
| B2.S4.bis | F2-alt FALLBACK: explicit `await import('./sentry.server.config')` dentro register() guarded por runtime check, O Sentry.init inline en route handler como ultimo recurso (pattern R1 SHIPPED como prueba pipeline funciona)            | Idem B2.S3.bis entregable                                                               | 75 min (nominal 15)    | B2.S3.bis FAIL | Deploy + verify FAIL → trigger Plan B (NO intentar F3 SDK upgrade)             |
| B2.S5.bis | Sentry.logger.\* smoke test bundled B3: post-S3.bis o S4.bis SHIPPED, 1 Sentry.logger.info() en handler + verify Sentry Logs dashboard                                                                                                  | Sentry Logs query con log_entry visible <2min                                           | 15 min (nominal 3)     | S3/S4 PASS     | Sentry Logs no disponible tier actual → defer B3 BACKLOG explicit              |
| B2.S6.bis | Branch cleanup + merge: revert console.log probes + delete diagnostic endpoints (verify-b2/debug-env/debug-send) + squash 9+N commits → 1-2 commits utiles + PR                                                                         | git log clean + PR descripcion + CI green                                               | 30 min (nominal 6)     | S5.bis PASS    | CI FAIL post-squash → preservar branch sin merge, ASK CEO                      |

**Total realistico v2**: 200 min (~3h20m). Total nominal: 40 min. Mismo techo v1 (variance budget intacto, contenido cambia).

**NOTA emergent sub-path Turbopack disable**: NO entra como sub-paso dedicado. Si B2.S3.bis OR B2.S4.bis revelan empirico que root cause = Turbopack instrumentation routing bug, escalar a Plan B (NO scope creep mid-fase intentar config refactor build pipeline).

---

## 2. Scope-lock v2 RE-EVALUADO

### IN scope v2 (taxativo)

- Probe register() invocation + fix instrumentation.ts pattern Next.js 16 (B2.S2.bis-S3.bis)
- Fallback explicit dynamic import en register() O Sentry.init inline pattern R1 (B2.S4.bis)
- Sentry.logger.\* smoke bundled B3 (B2.S5.bis)
- Branch cleanup + squash + merge main bajo Sec 2.d (B2.S6.bis)
- Update session-boot.md INMEDIATO cada sub-paso transicion

### OUT scope v2 (taxativo — redirect BACKLOG si emerge)

- **F3 upgrade @sentry/nextjs 10.x → 11.x**: CONFIRMA OUT esta sesion (supply-chain T4 ASK CEO no procesado, defer post-B2 dedicated session)
- **Sub-path Turbopack disable production build**: CONFIRMA OUT como fix dedicado (escala Plan B si empirico apunta ahi, NO refactor build pipeline mid-B2)
- Refactor instrumentation pattern global (solo minimum-change pipeline existente)
- Otras rutas API (solo verify-b2 + R1 + 1 endpoint post-merge smoke)
- Sentry performance/tracing/profiling instrumentation
- Sentry alerts/dashboards/workflows setup
- Replay/Session recording
- B3 Logflare/Axiom (descartado Ruta 0c, NO re-abrir)
- Supabase Log Drain $60/mes (Plan B Ruta 2 cubre)
- Refactor BACKLOG previas o cleanup endpoints OTROS workstreams
- Sentinel allowlist env template fix (entry separada, defer)

---

## 3. Variance budget triple refresh

| Tier         | Time  | Trigger                                                                              | Consumido este turn                                                     |
| ------------ | ----- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------- |
| Optimista    | 1h    | B2.S2.bis revela register() invoked + B2.S3.bis fix primer intento captura + cleanup | ~30 min wall-clock                                                      |
| Realistico   | 3h20m | B2.S2.bis + B2.S3.bis FAIL → B2.S4.bis fallback captura → cleanup 1 iter CI          | (Squad reconvene + B2.S1 baseline + ASK CEO chain)                      |
| Hard ceiling | 6h40m | 2x realistico → AUTO-ABORT sesion completa → Plan B Ruta 2 defer total               | Remanente real: 1h30m optimista / 2h50m realistico / 6h10m hard ceiling |

Hard ceiling = 10x nominal sub-paso individual O 2x realistico total. Lo que dispare primero abort.

---

## 4. Plan B trigger condition refresh (binary explicit)

**Cualquiera dispara, NO ambiguidad**:

1. B2.S3.bis AND B2.S4.bis ambos FAIL verify empirico (deploy SHIPPED pero 0 events Sentry post-fix)
2. Hard ceiling 6h40m alcanzado (cuenta desde inicio Squad reconvene previo)
3. B2.S2.bis revela register() invoked + server config carga + transport igual FAIL → indica SDK 10.53.1 + Next.js 16 incompatibilidad core requiere F3 upgrade (OUT scope confirmed)
4. B2.S3.bis OR B2.S4.bis revelan empirico root cause = Turbopack instrumentation routing bug requiere config refactor build pipeline (OUT scope confirmed)

**Ship al sacar Plan B**:

1. BACKLOG entry: W2.bis-B2 status `DEFERRED-PLAN-B-RUTA-2` + evidencia chain preservada branch + razon trigger concreto
2. session-boot.md: `current_phase: PLAN-B-RUTA-2-MANAGED-DASHBOARD` + next_action defined (verify Supabase managed dashboard 7d retention Pro plan + smoke query 1 log)
3. Telegram CEO + mirror chat: `B2 DEFERRED Plan B. Trigger: <cual>. Sentry Pro $25/mes mantener (errors+traces app-side via inline R1 pattern). Server-side logs Supabase dashboard 7d. Re-evaluar Log Drain $60/mes O F3 SDK upgrade post-launch volumen real.`
4. NO merge feature/sentry-verify-b2 a main bajo Plan B → branch preservada retry post-SDK upgrade

---

## 5. Checkpoint mid-sesion protocol (REGLA 1 CEO) — UPDATED

**Trigger update INMEDIATO `D:\impluxa-web\.planning\session-boot.md` seccion `Fase B2 progress`**:

- Cada sub-paso B2.SX.bis transicion PENDING → SHIPPED o FAIL (sin batch end-of-session)
- Cada hipotesis register-invoked / server-config-loaded / etc descartada empirico (con evidencia Vercel logs grep)
- Cada commit pusheado branch feature/sentry-verify-b2
- Trigger Plan B disparado (condicion concreta + timestamp)

**Formato linea**: `<HH:MM> B2.SX.bis <STATUS> — <evidence file/endpoint/query>`

**Telegram CEO solo en**:

- B2.S6.bis SHIPPED (merge main confirmed) — reporte post-ejecucion Sec 2.d
- Plan B disparado — Sec 4 ship list
- Hard ceiling alcanzado mid-sub-paso — ASK CEO continuar vs abort
- NUEVO: B2.S2.bis revela register() never-called (es milestone arquitectural, merece heads-up CEO unica linea)

NO Telegram per-sub-paso intermedio (evitar spam, log session-boot suficiente).

---

## 6. Two-Pass Review applicability per Sec 8 CLAUDE.md

| Sub-paso  | TP-extended required? | Justificacion                                                                                                                                                                                           |
| --------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B2.S2.bis | NO                    | Probe diagnostic read-only-ish (1 console.log add). No mutation arquitectural.                                                                                                                          |
| B2.S3.bis | NO                    | Minimum-change fix instrumentation pattern. NO categoria Sec 8 lista corta (no merge main, no migration, no propuesta arquitectura nueva). Squad decide aplicable: NO (fix LEAN dirigido por empirico). |
| B2.S4.bis | SI                    | Fallback dynamic import O Sentry.init inline = decision pattern arquitectural alterna. Pass-2 cold por Backend Architect agent fresh diferente.                                                         |
| B2.S5.bis | NO                    | Smoke test 1 llamada Sentry.logger.info(), read-only ingest verify.                                                                                                                                     |
| B2.S6.bis | SI                    | Merge a main bajo Sec 2.d = lista corta Sec 8 obligatoria. Pass-2 cold por Security Engineer agent fresh diferente review squash commits + PR descripcion + cleanup endpoints diagnostic.               |

---

**Pre-send**: PASSED (Sec 0+8 vocab + scope IN/OUT taxativo post-empirical + variance refresh con consumido turn + Plan B trigger binary 4 condiciones + checkpoint protocol REGLA 1 + Two-Pass per sub-paso justificado + cero hedges).

**Veredicto literal**: recomendamos proceder.
