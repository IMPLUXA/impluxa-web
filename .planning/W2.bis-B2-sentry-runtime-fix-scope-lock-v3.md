# W2.bis-B2-SENTRY-RUNTIME-CAPTURE-FIX — Scope Lock v3 POST-EDGE-EVIDENCE

**Owner**: Senior PM FRESH #3 (Squad T2 W2.bis-B2 re-reconvene, agentId fresh esta sesion)
**Date**: 2026-05-24
**Branch**: feature/sentry-verify-b2
**Flag**: hakuna_live=false
**Supersedes**: v2 (B2.S2.bis SHIPPED + hipotesis 'node' vs 'nodejs' REFUTADA + root cause EDGE runtime CONFIRMED)
**Veredicto**: recomendamos proceder

---

## 0. Empirical findings que invalidan plan v2

- NEXT_RUNTIME='edge' confirmado runtime route (probe deploys 2)
- `export const runtime = "nodejs"` IGNORADO por middleware.ts proxy semantics Next.js 16
- Sentry onRequestError handler = server-only (no edge support vendor docs context7)
- R1 manual `Sentry.captureException()` inline funciona = pipeline transport OK app-side
- middleware.ts default EDGE runtime fuerza routes downstream EDGE inherit

**Implicacion**: hipotesis register()/server.config layer no aplica. Edge runtime requiere pattern manual captureException (vendor-documented) O remove middleware EDGE proxy.

---

## 1. Sub-pasos B2.tris re-decomposition (post-edge)

| ID         | Objetivo                                                                                                                                                                                                                                                        | Entregable observable                                                                                                    | Realistico (5x) | Deps         | Abort criteria                                                                  |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | --------------- | ------------ | ------------------------------------------------------------------------------- |
| B2.S3.tris | F2 ELEGIR FIX path binary: A) manual Sentry.captureException wrapper helper edge-safe per route + apply verify-b2 + 1 ruta smoke. B) remove middleware.ts EDGE proxy → force nodejs default routes. Decision: A (lean, route-scope, NO refactor build pipeline) | Helper `src/lib/sentry-edge-capture.ts` + verify-b2 wrapped + deploy + curl + Sentry events query event_id != null <2min | 50 min (nom 10) | —            | Deploy FAIL 2x consec O fix aplicado + 0 events Sentry post-verify → B2.S4.tris |
| B2.S4.tris | F2-alt FALLBACK: si A FAIL, expandir helper a Sentry.init({ dsn, ...edgeConfig }) inline route-level pattern R1-style (yatested SHIPPED previo)                                                                                                                 | Idem entregable B2.S3.tris                                                                                               | 45 min (nom 9)  | S3.tris FAIL | Deploy + verify FAIL → Plan B (NO intentar middleware remove mid-fase)          |
| B2.S5.tris | Sentry.logger.\* smoke bundled B3 (post-S3 o S4 SHIPPED)                                                                                                                                                                                                        | Sentry Logs query log_entry visible <2min                                                                                | 15 min (nom 3)  | S3/S4 PASS   | Sentry Logs tier no disponible → defer B3 BACKLOG explicit                      |
| B2.S6.tris | Branch cleanup + merge: revert probes + delete diagnostic endpoints + squash → PR                                                                                                                                                                               | git log clean + PR + CI green                                                                                            | 30 min (nom 6)  | S5.tris PASS | CI FAIL post-squash → preservar branch, ASK CEO                                 |

**Total realistico v3**: 140 min (~2h20m). Total nominal: 28 min.

**REDUCED vs v2**: scope acotado 200→140min porque root cause confirmed elimina branches diagnostic + B2.S2.bis SHIPPED ya.

---

## 2. Scope-lock v3 RE-EVALUADO

### IN scope v3 (taxativo)

- Crear helper `src/lib/sentry-edge-capture.ts` con `captureEdgeError(err, ctx)` wrapper try/catch + manual Sentry.captureException pattern edge-safe
- Aplicar wrapper a `/api/sentry-verify-b2` + 1 ruta smoke adicional production-like
- Sentry.logger.\* smoke bundled B3 (1 llamada verify ingest)
- Branch cleanup + squash + merge main bajo Sec 2.d
- Update session-boot.md INMEDIATO cada sub-paso transicion

### OUT scope v3 (taxativo — BACKLOG si emerge)

- **Middleware.ts → proxy migration o remove**: BACKLOG entry separada `BACKLOG-W2.bis-middleware-edge-eval` (sub-finding documentado, defer dedicated session post-B2 ship). Razon: afecta routing prod indirecto, requiere ECC assessment, OUT scope fix lean B2.
- **F3 @sentry/nextjs 10.x → 11.x upgrade**: confirma OUT (supply-chain T4 ASK CEO no procesado).
- **Hybrid runtime + pattern per runtime (Fix3 BA shortlist)**: confirma OUT (over-engineering, A cubre caso 99%).
- **Refactor instrumentation pattern global, otras rutas API beyond 2, performance/tracing/profiling, alerts/dashboards, Replay, B3 Logflare/Axiom (Ruta 0c descartado), Supabase Log Drain $60/mes (Plan B Ruta 2), Sentinel allowlist env template fix**.

---

## 3. Variance budget triple refresh CONSUMIDO + RESTANTE

| Tier         | Time orig | Consumido turn | Restante real | Trigger                                                             |
| ------------ | --------- | -------------- | ------------- | ------------------------------------------------------------------- |
| Optimista    | 1h        | ~2h            | 0h (excedido) | B2.S3.tris primer intento + cleanup limpio                          |
| Realistico   | 3h20m     | ~2h            | ~1h20m        | S3.tris + S4.tris fallback + cleanup 1 iter CI                      |
| Hard ceiling | 4h        | ~2h            | ~2h           | CEO directive 4h hard cap → AUTO-ABORT sesion → Plan B Ruta 2 defer |

**Cap CEO refresh**: 4h hard (no 6h40m v1/v2). Restante real ~2h. Realistico v3 140min CABE en hard ceiling pero APRETADO. Optimista ya excedido — no plan optimista posible.

**Time-to-decision**: S3.tris fix A apply + deploy + verify = ~50min realistico. Si FAIL → S4.tris fallback 45min realistico = ~95min total. Plan B trigger en min 120 si S4 FAIL O Sentry events query 0 post-S4.

---

## 4. Plan B trigger condition v3 (binary explicit)

**Cualquiera dispara, NO ambiguidad**:

1. B2.S3.tris AND B2.S4.tris ambos FAIL verify empirico (deploy SHIPPED, 0 events Sentry post-fix wrapped routes)
2. Hard ceiling 4h CEO-imposed alcanzado (cuenta desde inicio sesion wall-clock)
3. S3.tris O S4.tris revelan empirico que helper edge-capture requiere @sentry/nextjs >=11 API surface no disponible 10.53.1 → F3 upgrade OUT confirmed
4. Mid-deploy emerge bug routing middleware separate que bloquea verify (escala Plan B, NO refactor middleware mid-fase)

**Ship Plan B v3** (igual a v2):

1. BACKLOG entry W2.bis-B2 `DEFERRED-PLAN-B-RUTA-2` + razon trigger
2. session-boot.md `current_phase: PLAN-B-RUTA-2-MANAGED-DASHBOARD` + next_action
3. Telegram CEO + mirror: `B2 DEFERRED Plan B. Trigger: <cual>. Sentry Pro $25/mes mantener (errors app-side R1 inline pattern usable). Server-side via Supabase dashboard 7d. Re-evaluar Log Drain $60/mes O F3 upgrade post-launch.`
4. BACKLOG separada middleware.ts edge eval defer
5. NO merge branch bajo Plan B → preservar retry post-SDK upgrade

---

## 5. Checkpoint mid-sesion protocol (REGLA 1 CEO)

**Update INMEDIATO `D:\impluxa-web\.planning\session-boot.md` seccion Fase B2 progress**:

- Cada B2.SX.tris PENDING → SHIPPED o FAIL
- Cada hipotesis fix-A-works / fix-A-fails-A-applied / etc descartada con evidencia
- Cada commit pusheado
- Plan B disparado (condicion + timestamp)

**Formato**: `<HH:MM> B2.SX.tris <STATUS> — <evidence>`

**Telegram CEO solo en**:

- B2.S6.tris SHIPPED (merge main confirmed) — reporte Sec 2.d
- Plan B disparado — Sec 4 ship list
- Hard ceiling 4h alcanzado mid-sub-paso — ASK CEO continuar vs abort
- B2.S3.tris SHIPPED milestone (root cause empirico validation, heads-up unica linea)

NO Telegram per-sub-paso intermedio.

---

## 6. Two-Pass Review applicability per Sec 8 CLAUDE.md

| Sub-paso   | TP-extended? | Justificacion                                                                                                                                                                              |
| ---------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| B2.S3.tris | SI           | Helper edge-capture nuevo + pattern per route handler edits multi-archivo (helper + 2 routes minimum) = decision pattern arquitectural. Pass-2 cold por Backend Architect fresh diferente. |
| B2.S4.tris | SI           | Fallback inline Sentry.init route-level = decision pattern alterna. Pass-2 cold mismo Backend Architect fresh (puede ser misma sesion, agent reconvene).                                   |
| B2.S5.tris | NO           | Smoke read-only ingest verify 1 llamada.                                                                                                                                                   |
| B2.S6.tris | SI           | Merge main Sec 2.d = lista corta Sec 8 obligatoria. Pass-2 cold por Security Engineer fresh review squash + PR + cleanup endpoints diagnostic.                                             |

**Sub-finding middleware deprecated**: BACKLOG entry sin TP (no mutation esta sesion, solo documentacion defer).

---

**Pre-send**: PASSED (Sec 0+8 vocab + scope IN/OUT taxativo post-edge-evidence + variance refresh consumido/restante + Plan B trigger binary 4 condiciones + checkpoint REGLA 1 + Two-Pass per sub-paso + cero hedges + sub-finding middleware redirected BACKLOG, NO scope creep).

**Veredicto literal**: recomendamos proceder.
