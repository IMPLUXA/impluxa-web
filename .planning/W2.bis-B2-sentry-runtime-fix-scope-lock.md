# W2.bis-B2-SENTRY-RUNTIME-CAPTURE-FIX — Scope Lock + Decomposition

**Owner**: Senior PM (Squad T2 W2.bis-B2)
**Date**: 2026-05-24
**Branch**: feature/sentry-verify-b2 (9 commits preservados)
**Flag**: hakuna_live=false
**Veredicto**: recomendamos proceder

---

## 1. Sub-pasos B2 (decomposition verificable)

| ID    | Objetivo                                                                                                                                                                        | Entregable observable                                                                    | Tiempo realistico (5x) | Deps            | Abort criteria                                                                              |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- | ---------------------- | --------------- | ------------------------------------------------------------------------------------------- |
| B2.S1 | Baseline empirico: rebuild branch + invocar 3 endpoints (R1 inline / verify-b2 canonical / debug-send) y comparar outputs Sentry events query                                   | Tabla 3 rows con event_id o NULL por endpoint + screenshot Sentry Issues last 5min       | 20 min (nominal 4)     | —               | Baseline NO reproduce R1=PASS rest=0 → entorno cambio → re-spec antes seguir                |
| B2.S2 | Hipotesis H5 narrow: leer instrumentation.ts + sentry.server.config.ts + next.config.\* + diff vs canonical Sentry docs Next.js 16 exacto                                       | Diff documento + lista discrepancias concretas (no genericas)                            | 30 min (nominal 6)     | B2.S1           | Diff = 0 discrepancias vs docs canonicas → H5 falso → re-hipotesis (skip a B2.S5)           |
| B2.S3 | Fix #1 (LEAN): aplicar minimum-change segun diff B2.S2 (ej: import order, runtime export, register() signature, env var name). Commit isolated.                                 | Vercel deploy SHIPPED + curl verify-b2 endpoint + Sentry query event_id != null en <2min | 45 min (nominal 9)     | B2.S2           | Deploy FAIL 2 intentos consecutivos build error → revertir commit → B2.S4                   |
| B2.S4 | Fix #2 (FALLBACK): si #1 no captura, swap a pattern alternativo documented (ej: sentry.server.config.ts top-level vs register() / explicit Sentry.init en cada API route layer) | Idem B2.S3 entregable                                                                    | 60 min (nominal 12)    | B2.S3 FAIL      | Fix #2 deploy + verify FAIL → trigger Plan B (NO intentar Fix #3)                           |
| B2.S5 | Sentry.logger.\* smoke test (B3 bundled): post-B2.S3 o S4 SHIPPED, agregar 1 Sentry.logger.info() en route handler + verify Logs tab Sentry dashboard                           | Sentry Logs query con log_entry visible <2min                                            | 15 min (nominal 3)     | B2.S3 o S4 PASS | Sentry Logs NO disponible plan Pro (verify dashboard tier) → defer B3 explicit nota BACKLOG |
| B2.S6 | Branch cleanup + merge: revert console.log probes + delete diagnostic endpoints (verify-b2/debug-env/debug-send) + squash 9 commits → 1-2 commits utiles + PR                   | git log clean + PR descripcion + CI green                                                | 30 min (nominal 6)     | B2.S5 PASS      | CI FAIL post-squash → preservar branch sin merge, ASK CEO                                   |

**Total realistico**: 200 min (~3h20m). Total nominal: 40 min.

---

## 2. Scope-lock

### IN scope (taxativo)

- Fix runtime capture pipeline `@sentry/nextjs` 10.53.1 + Next.js 16.2.6 con minimum-change desde codebase actual branch
- Verify empirico cada fix via endpoint hit + Sentry Issues query (NO trust deploy success solo)
- Sentry.logger.\* smoke test bundled (1 sola llamada B3 minimal verify Logs ingest)
- Branch cleanup + squash + merge a main (bajo Sec 2.d hakuna_live=false + Squad unanime + tests verdes)
- Update session-boot.md + BACKLOG cada sub-paso SHIPPED o FAIL

### OUT scope (taxativo — redirect a nueva BACKLOG entry si emerge)

- Upgrade `@sentry/nextjs` 10.x → 11.x (supply-chain assessment ECC formal pendiente, NO esta sesion)
- Refactor instrumentation pattern global (solo minimum-change al pipeline existente)
- Otras rutas API (solo endpoint verify-b2 + R1 + 1 endpoint post-merge smoke)
- Sentry performance/tracing/profiling instrumentation
- Sentry alerts/dashboards/workflows setup
- Replay/Session recording features
- B3 Logflare/Axiom path (descartado decision Ruta 0c) — NO re-abrir
- Supabase Log Drain $60/mes activacion (Plan B Ruta 2 cubre defer pre-launch)
- Refactor BACKLOG entries previas o cleanup endpoints OTROS workstreams
- Sentinel allowlist env template fix (entry separada s_B2_sentry_setup_paso1, defer)

---

## 3. Variance budget triple

| Tier         | Time  | Trigger                                                                |
| ------------ | ----- | ---------------------------------------------------------------------- |
| Optimista    | 1h    | Fix #1 (B2.S3) primer intento captura + cleanup limpio                 |
| Realistico   | 3h20m | Fix #1 FAIL → Fix #2 captura → cleanup con 1 iteracion CI              |
| Hard ceiling | 6h40m | 2x realistico → AUTO-ABORT sesion completa → Plan B Ruta 2 defer total |

Hard ceiling = 10x nominal sub-paso individual + 2x realistico total. Lo que dispare primero abort.

---

## 4. Plan B trigger explicit

**Condicion concreta (cualquiera dispara, NO ambiguidad)**:

- Fix #1 (B2.S3) AND Fix #2 (B2.S4) ambos FAIL verify empirico (deploy SHIPPED pero 0 events Sentry)
- OR hard ceiling 6h40m alcanzado independiente de progreso
- OR Fix shortlist requiere `@sentry/nextjs` upgrade major (OUT scope confirmed)
- OR diff B2.S2 = 0 discrepancias y baseline B2.S1 confirma R1=PASS canonical=FAIL (pipeline opaco no debuggable lean)

**Ship al sacar Plan B**:

1. BACKLOG entry update: W2.bis-B2 status `DEFERRED-PLAN-B-RUTA-2` + evidencia chain preservada branch
2. session-boot.md `current_phase: PLAN-B-RUTA-2-MANAGED-DASHBOARD` + next_action defined (verify Supabase managed dashboard 7d retention activo Pro plan, smoke query 1 log)
3. Telegram CEO + mirror chat: `B2 DEFERRED Plan B. Razon: <cual trigger>. Sentry Pro $25/mes mantener (errors+traces app-side via inline init R1 pattern usable). Server-side logs via Supabase dashboard 7d. Re-evaluar Log Drain $60/mes post-launch volumen real.`
4. NO merge feature/sentry-verify-b2 a main bajo Plan B → branch queda preservada para retry futuro post-SDK upgrade

---

## 5. Checkpoint mid-sesion protocol (REGLA 1 CEO)

**Trigger update INMEDIATO `D:\impluxa-web\.planning\session-boot.md` seccion `Fase B2 progress`**:

- Cada sub-paso B2.SX transicion PENDING → SHIPPED o FAIL (sin batch end-of-session)
- Cada hipotesis H5/H6/... descartada (con evidencia diff)
- Cada commit pusheado branch feature/sentry-verify-b2
- Trigger Plan B disparado (condicion concreta + timestamp)

**Formato linea**: `<HH:MM> B2.SX <STATUS> — <evidence file/endpoint/query>`

**Telegram CEO solo en**:

- B2.S6 SHIPPED (merge main confirmed) — reporte post-ejecucion Sec 2.d
- Plan B disparado — Sec 4 ship list
- Hard ceiling alcanzado mid-sub-paso — ASK CEO continuar vs abort

NO Telegram per-sub-paso intermedio (evitar spam, log session-boot suficiente).

---

**Pre-send**: PASSED (Sec 0+8 vocab + scope IN/OUT taxativo + variance realista + Plan B trigger binary + checkpoint protocol explicit + cero hedges veredicto).
